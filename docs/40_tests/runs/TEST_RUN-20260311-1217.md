# TEST_RUN-20260311-1217

- Run ID: `TEST_RUN-20260311-1217`
- Date: 2026-03-11
- Commit: `a4a2fa0`
- Operator: Codex
- Workbook Evidence: `docs/40_tests/testcases.xlsx`

## Scope

- Scenario IDs reviewed: `S1-S20`
- Execution mode: local offline verification only
- Sources used:
  - workbook data and historical runtime artifacts in `testcases.xlsx`
  - code/static contract scans in repo files

## Method

This run executed only checks that can be verified locally without a live Google Spreadsheet / Apps Script runtime.

Verification types used:

- workbook artifact inspection (`Snapshots`, `Monthly_Events`, `Dashboard_Snapshots`, `System_Tracking`, `Debug`)
- DTO/output payload inspection from `Dashboard_Snapshots.snapshot_json`
- code rule scan for canonicalization, PDF path, fallback path, and hard-rule output guards

## Scenario Status

- S1 Negative Net Portfolio: `PASS (offline evidence)`
  - Evidence: negative snapshot rows exist; latest dashboard snapshot contains `PortfolioLoss` action and recurring net `-400`
- S2 Signup Bonus Phase (PreBonus): `PASS (offline evidence)`
  - Evidence: `Snapshots` contains `Amex Business Gold` with `is_prebonus=true` and lifecycle `PreBonus`
  - Limitation: no UI screenshot / live rerun performed
- S3 Data Stale Detection: `BLOCKED`
  - Current workbook evidence shows `assets_days_since_confirmed=15`, `customer_data_status=Up to date`
  - No stale-data scenario present in latest accepted dashboard snapshot
- S4 Annual Fee Timing: `BLOCKED`
  - Code path for `FeeDue` exists, but workbook contains no `FeeDue` rows in `Monthly_Events`
  - Cannot force a fee-window rerun locally
- S5 Portfolio Optimization / Scenario Comparison: `PASS`
  - Evidence: latest `Dashboard_Snapshots.snapshot_json` contains both `scenario_comparison.do_nothing` and `.act`
  - UI template also renders from DTO source only
- S6 Long Promotion Wrap: `BLOCKED`
  - Requires UI/PDF visual inspection on a rendered report
- S7 Snapshot Pointer Repair (Legacy -> row_id): `BLOCKED`
  - Requires live Script Properties mutation and read-path repair
- S8 Empty Portfolio: `BLOCKED`
  - Current workbook has 1 active asset; no empty-portfolio runtime artifact available
- S9 Missing System Tabs Recovery: `BLOCKED`
  - Requires destructive sheet removal and live auto-create behavior
- S10 Multi-Employee Same Card Name: `BLOCKED`
  - Current active asset set has no duplicate active card names
  - Cannot synthesize and rerun event/snapshot writes locally
- S11 Event Alias Canonicalization: `PASS`
  - Code normalizes `annual_fee_due` and `feedue` to `FeeDue`
  - No `annual_fee_due` literal found in workbook dashboard payload / events
- S12 Output Hard Rules Enforcement: `PASS`
  - No `$0`, `-$0`, or `Check report details.` found in latest dashboard payload or workbook event artifacts
  - Code paths also render non-positive impact as `Potential upside`
- S13 DataStale vs Market Freshness Separation: `BLOCKED`
  - Latest dashboard snapshot shows separated fields, but only one non-stale state is present
  - Two controlled runs are still required
- S14 Same-Cycle Snapshot Reuse vs Force Regenerate: `BLOCKED`
  - Requires repeated live loads and forced regenerate behavior
- S15 System_Tracking Streak Progression: `BLOCKED`
  - `System_Tracking` contains only one row with `active_streak_months=1`
  - Multi-cycle progression cannot be verified from current workbook
- S16 Bleeding Dedupe / Retrigger / Cap: `BLOCKED`
  - Workbook shows Bleeding events exist, but dedupe/retrigger/cap needs repeated same-month live executions
- S17 Promotion Filtering and Cap: `BLOCKED`
  - Promotion catalog and `MarketWindow` events exist, but filter/cap correctness needs a controlled rerun
- S18 Backend PDF Template/Permission Failure Path: `BLOCKED`
  - Debug sheet shows a historical PDF URL, but not a controlled backend failure-path test
- S19 Required Sheets Presence Contract: `BLOCKED`
  - Requires renaming/removing a required operational sheet in a sandbox copy and rerunning
- S20 No Silent Fallback Acceptance: `FAIL`
  - `Debug` contains `ENV_LOCK` warnings for `Card_Catalog` and `Promo_Catalog`
  - Repo still contains multiple fallback success paths in `WebApp.gs`, so local evidence cannot prove accepted output came only from the main path

## Key Evidence

- Workbook evidence:
  - `Dashboard_Snapshots` latest row has dashboard DTO with `PortfolioLoss`, `scenario_comparison`, and `data_health`
  - `Snapshots` contains negative-net and `PreBonus` examples
  - `Monthly_Events` contains `Bleeding` and `MarketWindow` events
  - `Debug` includes:
    - `Monthly PDF: https://drive.google.com/file/d/1w8A5vFkAT44Mvm5A_RCc5jsdaSDtEz6K/view?usp=sharing`
    - `WARNING: show Card_Catalog failed: [ENV_LOCK] DATA_ENV=TEST forbids PROD sheet access...`

- Static/code evidence:
  - `ReportDTO.gs`, `Reports.gs`, and `PdfReports.gs` normalize fee-due aliases to canonical `FeeDue`
  - `BeautifulReportUI.html`, `Reports.gs`, and `PdfReports.gs` guard non-positive impact output with `Potential upside`
  - `WebApp.gs` still contains fallback success paths, so silent fallback remains a release-gate risk without live trace evidence

## Gate Summary

- Executed Result: `PARTIAL`
- Passed IDs: `[S1, S2, S5, S11, S12]`
- Failed IDs: `[S20]`
- Blocked IDs: `[S3, S4, S6, S7, S8, S9, S10, S13, S14, S15, S16, S17, S18, S19]`
- Overall Gate: `FAIL`

## Recommended Next Step

To unlock the blocked cases, the next run needs a real Spreadsheet / Apps Script execution path so we can:

- rerun Dashboard generation
- force snapshot regeneration
- mutate system-managed sheets in a sandbox copy
- capture live non-fallback traces
